# ast-grep configuration for wezterm_automata
# Enforces pattern code organization and bans ad-hoc regex usage
#
# Run: ast-grep scan --rule rules/
# Or:  ./scripts/lint-patterns.sh

ruleDirs:
  - rules
